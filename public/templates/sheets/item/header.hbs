<header>
	<img
		src='{{item.img}}'
		data-edit='img'
		data-action='editImage'
		title='{{item.name}}'
		alt='{{item.name}}'
	/>
	<span class='type'>{{typeLabel}}</span>
	<input
		type='text'
		name='name'
		value='{{item.name}}'
		placeholder='{{localize "Infinity.Labels.Name"}}'
		aria-label='{{localize "Infinity.Labels.Name"}}'
		{{disabled (not editable)}}
	/>
</header>